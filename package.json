{
  "name": "xv7-frontend-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('xv7 static frontend: no build step required')\"",
    "test": "vitest run",
    "test:browser": "playwright test",
    "gauntlet:browser": "playwright test e2e/xv7-browser-smoke.spec.mjs --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "jsdom": "^24.1.3",
    "vitest": "^2.1.8"
  }
}
